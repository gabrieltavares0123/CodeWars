<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/it_list_members_search"
        android:icon="@drawable/ic_search"
        android:title="@string/search"
        app:actionViewClass="androidx.appcompat.widget.SearchView"
        app:showAsAction="collapseActionView|ifRoom" />

    <item
        android:id="@+id/it_sort"
        android:icon="@drawable/ic_sort"
        android:title="@string/sort_options"
        app:showAsAction="always">
        <menu>
            <group>
                <item
                    android:id="@+id/it_by_search_date"
                    android:icon="@drawable/ic_search_date"
                    android:title="@string/sort_by_search_date" />
                <item
                    android:id="@+id/it_by_honor"
                    android:icon="@drawable/ic_honor"
                    android:title="@string/sort_by_honor" />
            </group>
        </menu>
    </item>
</menu>